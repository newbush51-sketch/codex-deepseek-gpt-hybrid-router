
# Contributing

1. Create a branch from `main`.
2. Run `python -m pytest -q` before opening a pull request.
3. Keep services bound to localhost and keep secrets out of code, arguments, and logs.
4. Make Windows paths portable; do not commit a personal username or application path.


